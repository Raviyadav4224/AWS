1. AWS CloudFormation -

    * It is an IaaS i.e it provides Infrastructure as a Service 

    * We can provision AWS resources using CloudFormation templates and it creates the resources in the 
      given order withe exact configuration
    
2. Benefits of CloudFormation -

    * IaaS - 

        No resources ar manaully created and can be version controlled
    
    * Cost -

        You can analyze the cost of resources being created beforehand, also you could automate the creation
        and deletion of your resources to save cost

    * Productivity -

        Ability to destroy and re-create an infrastructure on the cloud on the fly
        Automated generation of Diagram for your templates

    * Separation of concern -
    
        create many stacks for many apps, and many layers.
        Example - VPC stacks, Network stacks, App stack

3. How CloudFormation Works -

  * All the Templates must be uploaded in S3 and from then referenced in CloudFormation

  * To update a template we cannot edit the previous template, we have to upload a new version to S3 and
    then reference it
  
  * Stack = Single unit to manage, provision & update Collection of AWS resources

  * Stacks are identified by name, Deleting a Stack deletes all the underlying resources created by 
    CloudFormation

4. CloudFormation Templates & its Components -

  * Template’s Components -

      * AWSTemplateFormatVersion – 
          
        It is just a version marker for the template format
        It tells AWS which template syntax rules applies
        AWSTemplateFormatVersion: "2010-09-09"

      * Description – 
      
        Readme of a template, just to understand what this template does

      * Resources (MANDATORY) – 
          
        Defines actual AWS resources, CloudFormation creates/deletes/updates based on this
        Resources can be declared and reference each other
        Resource types identifiers are of the form: service-provider::service-name::data-type-name
        Example -
            Resources:
                MyEC2Instance:
                  Type: AWS::EC2::Instance
                  Properties:
                    InstanceType: t2.micro
                    ImageId: ami-0abcdef12345

      * Parameters – 

        These are dynamic inputs which the template uses
        Example -
            Parameters:
              InstanceTypeParam:
                Type: String
                Default: t2.micro

            Use inside resource: InstanceType: !Ref InstanceTypeParam

        * Psuedo Parameters -

            AWS::AccountId - 123456789012

            AWS::Region - us-east-1

            AWS::StackId - arn:aws:cloudformation:us-east-1:123456789012:stack/MyStack/1c2fa620-982a-
            11e3-aff7-50e2416294e0

            AWS::StackName - MyStack

            AWS::NotificationARNs - [arn:aws:sns:us-east-1:123456789012:MyTopic]

            AWS::NoValue - Doesn’t return a value

      * Mappings – 
          
          These are static values in the form of Key-Value pairs
          They are not dynamic like Parameters
          All the values are hardcoded within the template
          Example - 
              Mappings:
                RegionMap:
                  ap-south-1:
                    AMI: ami-123456
                  us-east-1:
                    AMI: ami-789012
                  
              Usage - ImageId: !FindInMap [RegionMap, !Ref "AWS::Region", AMI]

      * Outputs – 
          
          It returns useful values after stack creation
          Helps to get resource details
          It can be cross stack referenced as well
          Example - 
              Outputs:
              InstanceId:
                Description: "EC2 Instance ID"
                Value: !Ref MyEC2Instance
                Export:
                  Name: MyEC2InstanceId
          
          * Fn::ImportValue using this we can have cross stack reference
            Example - InstanceId: !ImportValue MyEC2InstanceId

          ** We cannot delete underlying stack unless all references are deleted
             If stack A gives outputs of its vpc id and stack B uses that, then we cannot directly
             delete A before, since it is being referenced in stack B

      * Conditionals – 
          
          Creates resources based on conditions just like if else
          Each condition can reference another condition, parameter value or mapping
          Example - 
              Parameters:
                EnvType:
                  Type: String
                  Default: dev

              Conditions:
                IsProd: !Equals [!Ref EnvType, prod]
              
              Resources:
                ProdOnlyBucket:
                  Type: AWS::S3::Bucket
                  Condition: IsProd

          * The intrinsic function (logical) can be any of the following 
            Fn::And, Fn::Not, Fn::Or, Fn::Equals, Fn::If

5. CloudFormation Intrinsic Functions -

  * There are various in-built Functions provided in cloudformation like -

    Fn::Ref, Fn::GetAtt, Fn::FindInMap, Fn::ImportValue, Fn::Join, Fn::Sub, Fn::ForEach, 
    Fn::ToJsonString, Condition Functions (Fn::If, Fn::Not, Fn::Equals, etc...), 
    Fn::Base64, Fn::Cidr, Fn::GetAZs, Fn::Select, Fn::Split, Fn::Transform, Fn::Length

  * Fn::Ref -
      
    Returns the value of PARAMETER or RESOURCE
    Example - 

    1. Resources:
          MyBucket:
            Type: AWS::S3::Bucket
          Outputs:
            BucketName:
              Value: !Ref MyBucket
        
    2.   Parameters:
          InstanceType:
            Type: String
            Default: t2.micro

        Resources:
          MyEC2:
            Type: AWS::EC2::Instance
          Properties:
            InstanceType: !Ref InstanceType

  * Fn::GetAtt - 

    Attributes are attached to any resources you create
    Works only on RESOURCES and NOT PARAMETERS
    Example - we created EC2 Instance, we can get its Attributes using !GetAtt MyEC2.PublicIp 

  * Fn::FindInMap - 

    * We use Fn::FindInMap to return a named value from a specific key
      !FindInMap [ MapName, TopLevelKey, SecondLevelKey ]

  * Fn::ImportValue -
      
    Import values that are exported in other stacks
    Example -  !ImportValue MyVPCId

  * Fn::Base64 -

    Convert String to it’s Base64 representation
    Example - 
        pass encoded data to EC2 Instance UserData property
        Resources:
        MyEC2:
            Type: AWS::EC2::Instance
            Properties:
            UserData:
                Fn::Base64: |
                #!/bin/bash
                yum install -y httpd
                systemctl start httpd

6. Handson -

  * Create an EC2 Instance with SecurityGroups -
  
    Template -

      AWSTemplateFormatVersion: '2010-09-09'
      Description: Creating EC2 instance with User Data Script encoded in base64

      Resources:
        pixxySSHSecurityGroup:
          Type: 'AWS::EC2::SecurityGroup'
          Properties:
            GroupDescription: Allow SSH access
            VpcId: vpc-05b59e4b9f1920285
            SecurityGroupIngress:
              - IpProtocol: tcp
                FromPort: 22
                ToPort: 22
                CidrIp: 0.0.0.0/0 

        pixxyEc2SecurityGroup:
          Type: AWS::EC2::SecurityGroup
          Properties:
            GroupDescription: Allow http/https on port 80
            VpcId: vpc-05b59e4b9f1920285
            SecurityGroupIngress:
              - IpProtocol: tcp
                FromPort: 80
                ToPort: 80
                CidrIp: 0.0.0.0/0

        pixxyEc2:
          Type: AWS::EC2::Instance
          Properties:
            SubnetId: subnet-0a2104e8b41dde1b3
            InstanceType: t2.micro
            ImageId: ami-0e12ffc2dd465f6e4
            KeyName: test_server_home
            SecurityGroupIds:
              - !Ref pixxyEc2SecurityGroup
              - !Ref pixxySSHSecurityGroup
            UserData: !Base64 |
              #!/bin/bash
              echo "Hello from CloudFormation" > /home/ec2-user/test.txt

7. CloudFormation Rollbacks -

  * Whenever stack creation or updation fails, CloudFormation automatically rollbacks to the last stable
    state

    Create fails - everything created is deleted
    Update fails - stack goes back to previous version
  
  * We can disable the rollback for debugging purpose

8. Deletion Policy of Resources -

  * We define deletion policy for resources which we create i.e what happens to them when we delete them
    or when a resource is removed from a CloudFormation template
    Example - 
    Resources:
      MyDB:
        Type: AWS::RDS::DBInstance
        DeletionPolicy: Retain
  
  * Possible values of DeletionPolicy - 
  
    * Delete (Default) - Resource is deleted
      * Delete won’t work on an S3 bucket if the bucket is not empty
    
    * Retain - Resource stays even if stack is deleted, Works on all resources

    * Snapshot - Creates one final Snapshot before deletion, Works on EBS, RDS, ElastiCache etc

9. Stack Policies -

  * A stack policy is JSON document that defines the UPDATE actions on RESOURCES when the stack is updated

  ** Default Denial: Once a stack policy is attached, CloudFormation denies any update to any resource in 
     the stack that is not explicitly allowed.

  * To allow updates for specific resources, you must include an explicit Allow statement for them in 
    the policy document.

  * If JSON policy has both ALLOW and DENY statement for the same resource, DENY wins

  * Example -
    {
      "Statement": [
        {
          "Effect": "Allow",
          "Action": "Update:*",
          "Principal": "*",
          "Resource": "LogicalResourceId/MyDB"
        }
      ]
    } 
    
10. Termination protection of Stack -

  * It protects accidental deletion of cloudformation stack

  * It can be set to ACTIVATED or DEACTIVATED

*11. Service Role & iam:PassRole -

  * Service Role -

    * It is the role assumed by CloudFormation to create/update/delete resources

    * It uses service role permissions instead of your IAM user permissions
    
    * Without service role - CloudFormation uses your iam user permissions

    * Give ability to users to create/update/delete the stack resources even if they don’t have 
      permissions to work with the resources in the stack
     
    * Service role has access to create EC2, S3, RDS
      Example - {
        "Action": [
          "ec2:*",
          "s3:*",
          "rds:*"
        ]
      }

  * iam:PassRole -

    * Permission that allows a user to pass an IAM role to CloudFormation, so CloudFormation can assume
      it and execute actions.

    * It is attached to IAM User

  * Example - 

    IAM user has:
      cloudformation:* // create/udpate/delete stack
      iam:PassRole
      
    Service role has:
      ec2:*
    
    CloudFormation can Create EC2, Cannot create S3, Cannot create ECS

12. Custom Resources in CloudFormation -
