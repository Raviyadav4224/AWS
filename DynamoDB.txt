1. What is DynamoDB -

  * It is a highly scalable, fully managed AWS service

  * It stores data in Key-Value pairs i.e it is a distributed key value store

  * It doesnot supports joins/aggregations like traditional SQL db or MongoDB 

2. Basics -

  * Tables - Like SQL Table

  * Item - Equivalent to Row i.e Row in a Table

  * Attributes - Keys in Object, which can differ per object since no fixed schema

  * Data Types -

    * Scalar Types – String, Number, Binary, Boolean, Null
    * Document Types – List(Array), Map(key-value)
    * Set Types – String Set, Number Set, Binary Set

  * Partition Key (Primary Key) -

    * For each item Partition key must be UNIQUE and diverse so that data can be distributed

    * DynamoDB internally HASHES the Partition key and then decide where Data is stored

    * Example - userId, empId etc

  * Partition Key + Sort Key (Composite Key) -

    * The combination must be UNIQUE for each item
      Example - userId + orderId
    
    * Items are grouped using the Partition Key

    * Items within the same partition are stored sorted by Sort key

3. Read/Write Capacity Modes -

  * Throughput - 
      
    * It is the Read/Write Capacity of DynamoDB i.e How many reads/writes per second the 
      table can process
      Example - 100 reads/sec, 50 writes/sec

    * Why Throughput exists ? Since everything has limit, like DynamoDB internal CPU, Network etc
      has fixed limits, So it must control how much data it can receive else any user can overload it

  * Provisioned Mode -

    * Here we decide the Capacity i.e say 50 reads/sec and 100 writes/sec
    
    * If the traffic exceeds Throtlling happens

    * Used mainly for stable workloads

  * OnDemand Mode -

    * We dont configure anything, AWS automatically scales reads/writes dynamically
      Unlimited WCU/RCU and No Throtlling
    
    * We pay for per request of Read/Write, 2.5 times more expensive compared to Provisioned

    * Use for unpredictable traffic, sudden spikes etc

  * Write Capacity Unit (WCU) -

    * It is defined as 1 Write per second for an item upto 1KB size, For items larger than 1KB
      more WCU are required

      Example - 

        Item size = 1KB, Need = 10 writes/sec, then required WCU = 10 WCU
        Item size = 4.5KB, Need = 6 writes/sec, then required WCU = 6 * 5/1 = 30 WCU

  * Read Capacity Units (RCU) -

    * It is defined as 1 Strongly Consistent Read or 2 Eventually Consistent Read per second for an 
      item of size upto 4KB
    
    * Example - 

        10 Strongly Consistent Reads per second, with item size 4 KB, We need 10 ∗ 4/4 = 10 RCU
        16 Eventually Consistent Reads per second, with item size 12 KB, we need (16/2) * (12/4) = 24RCU

  ** Partitions Internals -

    * Data is stored in partitions

    * DynamoDB uses Partition keys which further goes through Hashing algorithm to decide which partition
      to go
      Example - userId(101) - Hash(101) - Partition A, userId(102) - Hash(102) - Partition B
    
    * Rather than storing all the data in one storage, DynamoDB distributes the data in different storages
      known as Partitions
    
    * Since data is distributed in different partitions Read/Write can happen in parallel and hence DynamoDB
      scales massively
    
    * Partition keys are very important, because of this DynamoDB stores data in different partitions
      and bad designing of partition keys leads to overload of same partition with more traffic and 
      it leads to Hot Partition

    * If the table has 10,000 WCU/RCU and all traffic goes to same partition it can still throttle
      Since each partition has limits

      Read Capacity Units - 3000 RCU's
      Write Capacity Units - 1000 RCU's
      
  * Throtlling -

    * When available Capacity is less than the requested Capacity, DynamoDB rejects the request, it is
      known as Throtlling

    * When request throttles, we get ProvisionedThroughputExceededException 

  * Strongly Consistent Read vs Eventually Consistent Read -

    Eventually Consistent Read (Default) -

      If we read just after a write, it is possible we get some stale data because of replication
      It is faster and cheaper
      Use case - Social media likes, Products etc

    Strongly Consistent Read -

      You will always get the latest commited data
      It is slower compared to Eventually Consistent Read and consumes more RCU
      Use case - Banking, checking balance after transferring money needs latest data

  * Replication - 
    
    * By default DynamoDB replicates data across multiple AZs within a region.
    
    * For Cross region replication, we need to use Global tables, and this process in Async in nature
      so it is always Eventually Consistent Read

4. Reading/Writing in DynamoDB -

  * Writing -

    * PutItem (INSERT OR REPLACE) - 

      It replaces the older object or inserts the new one
    
    * UpdateItem (Udpate and NOT REPLACE) -

      * Updates the existing object

      * It supports Update expressions 

        | Operation | Meaning              |
        | --------- | -------------------- |
        | SET       | add/update attribute |
        | REMOVE    | remove attribute     |
        | ADD       | increment numbers    |
        | DELETE    | remove from set      |

      * It is ATOMIC in nature means that two concurrent requests wont interupt or corrupt each other

  * Reading -

    * GetItem -

      * Fetches one item using Primary key, Fastest Read Operation

      * If Primary and Sort key both exists, both need to be specified while getting the data

    * Query -

      * Fetches multiple related items using -
        Partition key value (required) (=)
        Sort key value (optional) (=, <, <=, >, >=, Between, Begins with)
      
      * Why Query is faster ? 
        
        It doesnot queries the whole table since DynamoDB knows the Partition using partition key
    
    * Scan -

      Scan the entire table and then filter out data (inefficient)

      It consumes more RCU's
    
  * Deleting -

    * DeleteItem -

      * Deletes an individual item
      * Ability to perform a conditional delete

    * DeleteTable -

      * Delete a whole table and all its items
      * Much quicker deletion than calling DeleteItem on all items

  * Batch Operations -

    * Allows us to reduce Number of API calls

    * BatchWriteItem

      * Supports multiple PutItem, multiple DeleteItem
      * Up to 25 items in single request.
      * UnprocessedItems for failed write operations 

    * BatchGetItem -

      * Return items from one or more tables
      * Up to 100 items, up to 16 MB of data
      * UnprocessedKeys for failed read operations

  * PartiQL -

    SQL like syntax for DynamoDB, under the hood it still uses DynamoDB rules

    Example - GetItem API, you can write - SELECT * FROM Users WHERE userId='101'

5. Conditional Operations -

  * Conditional Writes -

    * Used when data is being written in to the DB (PRE-WRITE)

    * attribute_exists(attribute) - Checks attribute exists or not, if not then doesnot proceeds

    * attribute_not_exists(attribute) - Inserts only when the attribute doesnot exists

    * attribute_type(age, 'N') - Checks attribute type, i.e here age must be Number

    * contains(tags, :tag) - Checks whether a substring exists within another string or not
      It is less efficient
      Not allowed in KeyConditionExpression

    * begins_with(tags, :tags) - Checks whether value STARTS with something.
      It is more efficient
      It is index-friendly
      It can be used with sort keys in KeyConditionExpression

  * KeyConditionExpression vs FilterExpression vs ConditionalExpression -

    KeyConditionExpression - 
    
      * It is PRE-READ, used for Quering operations using keys
      * It is the most efficient since it uses Primary and Secondary keys to locate the data

    FilterExpression -

      * Used with Query/Scans, to filter data after it is read

    ConditionalExpression -

      * It is PRE-WRITE, i.e it evaluated before writing in DB
      
    | Function             | KeyConditionExpression | FilterExpression | ConditionExpression |
    | -------------------- | ---------------------- | ---------------- | ------------------- |
    | begins_with          | ✅                      | ✅                | ✅                   |
    | contains             | ❌                      | ✅                | ✅                   |
    | attribute_exists     | ❌                      | ✅                | ✅                   |
    | attribute_not_exists | ❌                      | ✅                | ✅                   |
    | size                 | ❌                      | ✅                | ✅                   |
    | attribute_type       | ❌                      | ✅                | ✅                   |

  ** BatchWriteItem(supports put and delete), ConditionExpression is NOT supported, for that we have
     to use TransactWriteItems, This supports Put, Update, Delete, ConditionCheck

6. Handson Examples -

  * GetItem -

    aws dynamodb get-item \
    --table-name Users \
    --key '{
      "userId": {"S": "emp1"}
    }'
    
  * Update Existing Items -
    
   aws dynamodb update-item \
    --table-name Users \
    --key '{
      "userId": {"S": "emp1"}
    }' \
    --update-expression "SET salary = :newSalary" \
    --condition-expression "salary = :oldSalary" \
    --expression-attribute-values '{
      ":newSalary": {"N": "7000"},
      ":oldSalary": {"N": "5000"}
    }'

  * Insert Items -

    aws dynamodb put-item \
    --table-name Users \
    --item '{
      "userId": {"S": "emp1"},
      "name": {"S": "Aman"},
      "salary": {"N": "5000"},
      "department": {"S": "Engineering"}
    }'

  * DeleteItems -

    aws dynamodb delete-item \
    --table-name Users \
    --key '{
      "userId": {"S": "u-delete"}
    }' \
    --condition-expression "status = :s" \
    --expression-attribute-values '{
      ":s": {"S": "INACTIVE"}
    }'

7. Local Secondary Index vs Global Secondary Index -

  * Why these indexes are required ?
    
    DynamoDB queries the data faster using indexes like Primary and Secondary
    Now in order to query different data based on different attributes say base table PK userid, 
    SK orderId, To get all users with status PENDING - total scan would be required without using index, 
    so LSI and GSI exists to solve this problem
    
  * Local Secondary Index -

    * Same partition key as base table
    * Different sort key allowed
    * Created only during table creation
    * Supports strong consistency
    * Shares partitioning with base table
    * Good for alternate sorting/querying within same partition
    * Cannot change partition access pattern
    * 10GB partition limit applies
    * Usecase - Get all orders of USER U1 by status (since partition is same)
    * Uses the WCU/RCU's of the main table 

  * Global Secondary Index -

    * Different partition key allowed
    * Different sort key allowed
    * Can be added later
    * Separate storage and partitioning, Attribute Projections - i.e we can define the attributes to store 
      in new table, Either ALL, INCLUDE, KEYS_ONLY
    * Separate scaling and throughput
    * Enables completely new access patterns
    * Most commonly used DynamoDB index
    * Usecase - show ALL PENDING orders (old PK SK wont work and full table scan would be required)
    * WCU/RCU's must be configured differently for GSI, If GSI throttles during write operations, the 
      main table would also throttle, since each write to main table also writes to GSI

8. Optimistic Locking -

  * DynamoDB has a feature called Conditional Writes i.e checks the condition before writing anything
    into the DB, this is known as Optimistic Locking 
    i.e it checks the version number of the item and ensures it has not changed before update or delete
  
  * This version is to be maintained by Developer, DynamoDB itself doesnot provides any attribute

  * There is no actual locking like Row locking in traditional SQL db's

9. DynamoDB Accelerator (DAX) vs ElastiCache -

  * DynamoDB Accelerator (DAX) -

    * It is a fully managed service of AWS particularly for DynamoDB cache i.e it caches the result of
      DynamoDB queries mostly READ operations
    
    * It doesnot requires any application level logic modification, i.e just changing the DynamoDB client 
      with DAX client works perfectly
    
    * It solves the HOT KEY problem

    * We can define cache TTL (default 5minutes)

    * We can have upto 10 nodes in a Cluster, Supports Multi-AZ (minimum 3 nodes recommended for prod)

  * DAX vs ElastiCache -

    * DAX is for DynamoDB only whereas ElastiCache can be used to cache any thing like API responses etc

    * ElastiCache requires code modification like if redis.get(item) whereas in DAX nothing required

10. DynamoDB Streams -

  * Whenever an item is INSERTED, UPDATED or DELETED, DynamoDB Streams records that event
    Example - {
      "eventName": "MODIFY",

      "OldImage": {
        "UserId": "101",
        "Name": "Ravi"
      },

      "NewImage": {
        "UserId": "101",
        "Name": "Ravi Kumar"
      }
    }
     DynamoDB change => Lambda triggered 
     New order item received => send emails etc

  * Streams enables reacting to database changes automatically

  * We can configure what goes into the Streams -

    KEYS_ONLY – only the key attributes of the modified item is stored
    NEW_IMAGE – the entire item, as it appears after it was modified is stored
    OLD_IMAGE – the entire item, as it appeared before it was modified is stored
    NEW_AND_OLD_IMAGES – both the new and the old images of the item is stored
  
  * Streams captures events like INSERT, MODIFY, REMOVE

  ** For same partition key, Streams maintains order i.e Order#101 = CREATED => PAID => SHIPPED

  * DynamoDB Streams keeps records for 24 hours, After that records disappear permanently.

  * Streams are Asynchronous in nature i.e it doesnot waits for Lambda to finish

11. TTL in DynamoDB -

  * We can define TTL for items, so that it automatically gets removed without consuming any WCU's

  * TTL attribute MUST be Number type AND Unix Epoch Timestamp
    {
      "ExpiryTime": 1715800000
    }

  * Unix Epoch Timestamp Means Seconds since Jan 1, 1970 UTC
    Example:  1715800000

  * It doesnot deletes the data immediately after the expiry time, TTL is a Cleanup process
    Expired data may still be visible and might take minutes, hours or days to delete
    It becomes unavailable only after it is physically deleted

  * Expired items also gets deleted from LSI and GSI

  * DynamoDB Streams captures the events Whenever TTL Removes the items, based on that system can react

12. DynamoDB Transactions -

  * Each items operation is ATOMIC in dynamodb but multiple operations together are not ATOMIC
    i.e Operation 1 succeeds, Operation 2 may fail

  * DynamoDB Transactions solves this problem by providing All or Nothing behavior
    i.e All operations succeed or All operations rollback i.e No partial update
  
  * Transactions provides ACID properties i.e Atomicity, Consistency, Isolation and Durability

  * We have two main API's for this -

    TransactWriteItems - supports PutItem, UpdateItem, DeleteItem and ConditionCheck

    TransactGetItems - Atomic reads of multiple items 

  ** DynamoDB performs 2 operations per item PREPARE + COMMIT
     i.e for each item dynamodb roughly does prepare phase and then commit phase
     Therefor it consumes 2* WCU/RCU's
  
  * Useful in Financial transactions, Multiplayer games etc

13. DynamoDB as Session State Cache -

  * DynamoDB can be used to store user session data

  * DynamoDB vs ElastiCache vs EFS vs EBS vs S3 - 

    ElastiCache is not serverless whereas dynamodb is
    EFS must be attached to multiple EC2 instances
    EBS is for local caching
    S3 is for larger data and has more latency

14. DynamoDB Write Sharding -

  * Imagine we have a voting application with two candidates, candidate A and candidate B
    If Partition Key is Candidate_ID, this results into two partitions, which will generate issues 
    (e.g., Hot Partition)

  * Write Sharding is a strategy that allows better distribution of items evenly across partitions

  * Two methods: 
    
    Sharding Using Random Suffix
    Sharding Using Calculated Suffix

15. Write Tyeps in DynamoDB -

  * Concurrent Write - Two clients overwrite each other.
  * Atomic Write - requests dont interfere each other
  * Conditonal Write - writes happens conditionally
  * BatchWrite - writes many items at a time, some may fail as well

17. Storing Large Objects in S3 -

  * DynamoDB items size has a limit of 400KB, for larger items like videos, pdfs, images we can store
    them in S3 and store the Metadata inside DynamoDB

  * S3 alone is not enough for Metadata, so we genrally store searcheable metadata in dynamodb so that
    using indexes dynamodb can query the results faster
  
  * Example - 
    
    Client uploads file to S3 => S3 Event Notification => Lambda Triggered => Store metadata in DynamoDB

18. Security -

  * Security -
    
    VPC Endpoints available to access DynamoDB without using the Internet
    Access fully controlled by IAM
    Encryption at rest using AWS KMS and in-transit using SSL/TLS

  * Backup and Restore feature available

    Point-in-time Recovery (PITR) like RDS
    No performance impact

  * DynamoDB Local

    Develop and test apps locally without accessing the DynamoDB web service (without Internet)

  * AWS Database Migration Service (AWS DMS) can be used to migrate to DynamoDB (from MongoDB, 
    Oracle, MySQL, S3, ...)

19. Global Tables -

  * Global tables are used when we want to use Multi Region Active Active DynamoDB

  * Here same table gets replicated across multiple regions
    Read/Write is possible from any region
    Replication is automatic

  ** Global tables provides Eventually Consistency and table replication is Asynchronous in nature

  ** It solves the conflict using LAST WRITER WINS based on Timestamp i.e the one who writes 
     the data in the last wins

20. Parallel Scans -

  * Parallel scan allows multiple workers to scan different parts of table simultaneously thus increasing
    the efficiency and decrease the total time consumed
  
  * It uses totalSegments and Segments to do this
    Say totalSegments = 4 , So DynamoDB internally divides them into Segments with each worker using one
    Segments and combines the result in the end
  
  * Useful for full table reads, analytics, Backups, Migration jobs